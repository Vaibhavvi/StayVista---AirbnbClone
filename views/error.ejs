<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Error <%= err.statusCode || 500 %> | StayVista</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      background: radial-gradient(circle at top left, #ff6b6b, #f8d7da, #fcefee);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      position: relative;
      text-align: center;
      padding: 50px 40px;
      border-radius: 20px;
      backdrop-filter: blur(10px);
      background: rgba(255, 255, 255, 0.15);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
      color: #fff;
      animation: floatUp 1.8s ease-in-out forwards;
    }

    .error-code {
      font-size: 7rem;
      font-weight: 700;
      color: #ff4b5c;
      text-shadow: 0 0 20px rgba(255, 75, 92, 0.5);
      margin-bottom: 0.3em;
    }

    .error-message {
      font-size: 1.5rem;
      font-weight: 500;
      color: #2b2b2b;
      background: rgba(255, 255, 255, 0.65);
      display: inline-block;
      padding: 8px 16px;
      border-radius: 10px;
      backdrop-filter: blur(6px);
    }

    .btn {
      margin-top: 2em;
      background: linear-gradient(135deg, #ff4b5c, #ff7676);
      border: none;
      color: white;
      padding: 12px 28px;
      font-size: 1rem;
      border-radius: 50px;
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 5px 15px rgba(255, 75, 92, 0.4);
      transition: all 0.3s ease;
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(255, 75, 92, 0.5);
    }

    @keyframes floatUp {
      from { opacity: 0; transform: translateY(50px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Animated bubbles for “magic” effect */
    .bubble {
      position: absolute;
      bottom: -100px;
      width: 40px;
      height: 40px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: 50%;
      animation: rise 10s infinite ease-in;
    }

    .bubble:nth-child(1) { left: 15%; width: 30px; height: 30px; animation-delay: 1s; }
    .bubble:nth-child(2) { left: 35%; width: 50px; height: 50px; animation-delay: 3s; }
    .bubble:nth-child(3) { left: 55%; width: 20px; height: 20px; animation-delay: 2s; }
    .bubble:nth-child(4) { left: 75%; width: 40px; height: 40px; animation-delay: 4s; }
    .bubble:nth-child(5) { left: 85%; width: 60px; height: 60px; animation-delay: 6s; }

    @keyframes rise {
      0% { transform: translateY(0) scale(1); opacity: 1; }
      100% { transform: translateY(-800px) scale(1.3); opacity: 0; }
    }

  </style>
</head>

<body>
  <div class="container">
    <div class="error-code"><%= err.statusCode || 500 %></div>
    <div class="error-message"><%= err.message || "Something went wrong!" %></div>
    <a href="/listings" class="btn">⬅ Back to Listings</a>
  </div>

  <!-- Floating magic bubbles -->
  <div class="bubble"></div>
  <div class="bubble"></div>
  <div class="bubble"></div>
  <div class="bubble"></div>
  <div class="bubble"></div>
</body>
</html>
