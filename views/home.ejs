<% layout('layout/boilerplate') %>

<style>
  /* 🏡 Welcome Card Styling */
  .welcome-card {
    background: #fff;
    border-radius: 1.5rem;
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    padding: 3rem 2rem;
    max-width: 600px;
    width: 100%;
    text-align: center;
    transition: transform 0.3s ease;
  }

  .welcome-card:hover {
    transform: translateY(-5px);
  }

  .welcome-card h1 {
    font-weight: 700;
    color: #ff385c;
  }

  .welcome-card p {
    color: #555;
    font-size: 1.1rem;
  }

  /* ✨ Alert Styling */
  .alert {
    border-radius: 1rem;
    font-weight: 500;
    font-size: 1rem;
  }

  .btn-close {
    outline: none;
  }

  .brand-logo {
    font-size: 3rem;
    color: #ff385c;
    margin-bottom: 1rem;
  }
</style>

<div class="container">
  <div class="welcome-card mx-auto">
    <!-- 🌟 Logo -->
    <div class="brand-logo">
      <i class="bi bi-house-heart-fill"></i>
    </div>

    <!-- ✅ Flash Messages -->
    <% if (success && success.length) { %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <% if (error && error.length) { %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= error %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% } %>

    <!-- 🏡 Main Content -->
    <h1>Welcome to <span style="color: #ff385c;">StayVista</span>!</h1>
    <p class="mt-3 mb-4">Find the perfect place to stay, anywhere in the world 🌍</p>

    <a href="/listings" class="btn btn-lg btn-gradient text-white px-4 py-2" 
       style="background: linear-gradient(90deg, #ff385c, #ff6f61); border: none; border-radius: 30px;">
       Explore Listings
    </a>
  </div>
</div>
